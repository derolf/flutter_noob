name: noob_example
description: Example app for noob

publish_to: none
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.16.0
  flutter_markdown: ^0.6.1
  hooks_riverpod: ^0.14.0+2
  noob:
    path: ../

dev_dependencies:
  build_runner: ^2.0.1
  lint: ^1.5.3

flutter:
  uses-material-design: true
